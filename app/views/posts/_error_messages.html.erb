<% if object.errors.any? %>
  <h2><%= pluralize(object.errors.count, "エラー") %>があります:</h2>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
