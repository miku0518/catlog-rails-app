<p>最新の投稿</p>

<% @posts.each do |post| %>
  <div style="border:1px solid #ccc; padding:10px; margin-bottom:10px;">
    <div>
    <% if post.image.attached? %>
      <%= image_tag post.image, style: "width: 100px;" %>
    <% end %>
    <h2><%= post.title %></h2>
    <p>投稿者：<%= post.user.login_name %></p>
    <h3>コメント <%= post.comments.count %> </h3>
    <%= post.created_at.strftime("%Y/%m/%d %H:%M") %>
    <br>
    <%= link_to "続きを読む", post_path(post),
      class: "btn btn-outline btn-info" %>
  </div>
<% end %>

<%= link_to "投稿一覧を見る", posts_path %>
<p class="text-red-500 text-2xl">テスト</p>
<p class="text-red-500 text-2xl font-bold">TAILWIND TEST</p>
<p class="text-red-500 text-2xl font-bold">TAILWIND TEST</p>
<span class="loading loading-spinner loading-xl"></span>